# Results

`baseline_seed_42.json` is the versioned reference experiment documented in the main README. `latest_results.json` is overwritten on every run and excluded from Git. Versioned experiments should use descriptive filenames and record their complete configuration and seed.

